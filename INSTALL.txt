BizBuzz Studio - Farhan photo
=============================

   cd /d C:\BizBuzz\thebizbuzz-studio
   xcopy "C:\Users\DAR AL BARAKAH\Desktop\bizbuzz-farhan\*" "C:\BizBuzz\thebizbuzz-studio\" /E /Y
   rmdir /s /q .next
   npm run dev

The .next delete matters, Next caches optimised images hard.


THE CROP
--------
Source was 1024x1024 square, output 900x1125 portrait to match the card.
Crop biased 10 percent upward because his face sits high in the frame,
which keeps the composition from cutting at the chin.


ONE THING WORTH KNOWING
-----------------------
This photo is dark, moody and studio-lit on a near-black background.
Yours is the same. Amrit's is bright, grey and evenly lit. Ghosia's is
light and soft.

So the team row now reads as two dark portraits and two light ones,
which will look uneven side by side. It is the same issue I flagged
when the first two went in.

Two ways to solve it, whenever you want to:

   Brief Waqas to match one of the two looks, so the row is at least
   three consistent out of four.

   Or I apply a unified grade in code: desaturate slightly and add a
   subtle navy tint on all four. That pulls them together regardless of
   source and reads as a deliberate treatment rather than a mismatch.
   One line, reversible.

Say the word and I will add the grade. It is the cheaper fix and it
works no matter what the remaining photos look like.


STILL MISSING
-------------
Waqas Sheikh is the only partner still rendering as initials.
